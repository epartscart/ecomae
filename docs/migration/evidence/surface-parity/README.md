# Surface parity evidence

Attach:

- `harness-report.json` from `bash scripts/run_surface_parity_harness.sh`
- `presentation-asset-check.json`
- Dual samples under `samples/` named `{surface}-{route}-php.json` / `{surface}-{route}-aspnet.json`

Do not enable cutover until compare script reports PASS for each contracted digest.
