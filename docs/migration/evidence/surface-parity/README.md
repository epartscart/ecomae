# Surface parity evidence

Attach:

- `harness-report.json` from `bash scripts/run_surface_parity_harness.sh`
- `presentation-asset-check.json`
- Migration-mode contract samples under `samples/migration/` (no secrets; regenerated by harness)
- Dual authenticated samples under `samples/` named `{surface}-{route}-php.json` / `{surface}-{route}-aspnet.json`

Do not enable cutover until compare script reports PASS for each contracted digest against live dual samples.
