# Operator verify — catalog miss dual samples (Batch 5)

```bash
bash scripts/cloudpanel_run_catalog_miss_dual_sample_operator.sh
```

Default uses contract stubs without API key. Live UMAPI fill remains PHP-authoritative.
Expect `cutoverAllowed=false`. Never invent `RELEASE_OWNER_APPROVAL.md`.
