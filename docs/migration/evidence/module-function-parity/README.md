# Module function parity evidence

Contract floor only. Hybrid TARGETS are preview/deeplink coverage; full PHP catalog scope (CP/ERP/BOS/storefront counts) is tracked via `phpCatalogCounts` and remains PHP-authoritative. `aspnet-complete` count stays **0** until a human attaches `docs/migration/evidence/presentation/MODULE_FUNCTION_TEST_PASS.md` containing `MODULE_FUNCTION_PARITY_PASS`.

Never invent that pass file or `RELEASE_OWNER_APPROVAL.md`. `cutoverAllowed` always false.

Operator:

```bash
bash scripts/cloudpanel_run_module_function_parity_operator.sh
```
