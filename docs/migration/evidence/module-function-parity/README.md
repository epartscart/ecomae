# Module function parity evidence

Full PHP catalog inventory (CP/ERP/BOS/storefront) plus hybrid TARGET rows.

Current offline floor: **829/829** rows at `digest-only+hybrid-deeplink` (scaffold previews).
`aspnet-complete` count stays **0** until a human attaches
`docs/migration/evidence/presentation/MODULE_FUNCTION_TEST_PASS.md` containing `MODULE_FUNCTION_PARITY_PASS`.

Never invent that pass file or `RELEASE_OWNER_APPROVAL.md`. `cutoverAllowed` always false.

Operator:

```bash
bash scripts/cloudpanel_run_module_function_parity_operator.sh
```
