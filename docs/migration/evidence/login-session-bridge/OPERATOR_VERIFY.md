# Operator verify — login-cookie dual samples (Batch 3)

```bash
bash scripts/cloudpanel_run_login_cookie_dual_sample_operator.sh
```

Default refreshes contract stubs without credentials. Expect `cutoverAllowed=false`.
Never invent `RELEASE_OWNER_APPROVAL.md`.
