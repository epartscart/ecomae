@* Mirrors templates/nero/desktop.php GA4 + optional Clarity hooks. *@
@if (Enabled)
{
    <script async src="https://www.googletagmanager.com/gtag/js?id=@MeasurementId"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){ dataLayer.push(arguments); }
        gtag('js', new Date());
        gtag('config', '@MeasurementId');
    </script>
    @if (IncludeClarity && !string.IsNullOrWhiteSpace(ClarityId))
    {
        <script type="text/javascript">
            (function(c,l,a,r,i,t,y){
                c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
                t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
                y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
            })(window, document, "clarity", "script", "@ClarityId");
        </script>
    }
}

@code {
    [Parameter] public bool Enabled { get; set; } = true;
    [Parameter] public string MeasurementId { get; set; } = "G-J19D1KHXCG";
    [Parameter] public bool IncludeClarity { get; set; }
    /// <summary>Matches PHP <c>epc_portal_clarity_project_id()</c> for epartscart.com.</summary>
    [Parameter] public string ClarityId { get; set; } = "xoflbamawu";
}
