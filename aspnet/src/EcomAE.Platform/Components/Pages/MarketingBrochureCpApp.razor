@page "/marketing/brochure-cp"
@layout Layout.PhpChromeLayout
@using EcomAE.Platform.Presentation
@inject IHttpContextAccessor Http

<PageTitle>ECOM AE — Full CP brochure (PHP parity preview)</PageTitle>
<PhpChromeStyles Surface="marketing" />

<PhpEcomaeMarketingChrome ActiveKey="brochure_cp">
    <PhpHybridWorkspaceFrame PhpHref="@_phpHref" ClearHref="/marketing/brochure-cp" />

    @if (string.IsNullOrWhiteSpace(_phpHref))
    {
        <PhpEcomaeBrochureCpOverview />
    }
</PhpEcomaeMarketingChrome>

@code {
    private string? _phpHref;

    protected override void OnInitialized()
    {
        var ctx = Http.HttpContext;
        if (ctx?.Request.Query.TryGetValue("php", out var php) == true && !string.IsNullOrWhiteSpace(php))
        {
            _phpHref = php.ToString();
        }
    }
}
