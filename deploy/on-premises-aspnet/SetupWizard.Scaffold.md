Scaffold notes for replacing setup-wizard.php with an ASP.NET Core CLI/host worker.
Parity steps: verify core+config → run idempotent migrations → confirm license → point admin to /cp/ onboarding.
Live execution stays PHP until dual-sample.
